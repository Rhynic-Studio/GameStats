# 重做设计（待确认 —— 你点头之前不写代码）

## 一、目录结构

正常 Vite 项目。**没有 `games/` 目录、没有注册表、没有自动发现。**
crash 和 cs2 各写各的页面，共用的只有数据库、类型、基础控件。

```
index.html
vite.config.ts
package.json
src/
  server/
    index.ts        路由
    db.ts           sqlite + 建表
  client/
    main.tsx
    App.tsx         路由
    pages/
      Games.tsx         选择游戏
      crash/List.tsx    对局列表
      crash/Edit.tsx    录入
      crash/Stats.tsx   统计
      crash/Players.tsx 玩家 + 角色
      cs2/List.tsx
      cs2/Edit.tsx
      cs2/Stats.tsx
    components/     输入框、下拉、表格这些
  shared/
    types.ts
```

## 二、记什么

### crash

```
players        id, name
entities       id, game, name
crash_matches  id, date, p1, p2, 先手是哪边, 备注
crash_pool     对局id, 角色id                    ← log 里的「出现」
crash_draft    对局id, 第几个, ban还是pick, 哪边, 角色id   ← log 里的「先手1ban」
crash_rounds   对局id, 第几轮, 谁先攻, p1角色, p2角色, 胜方, 胜利条件
```

三层就是 log 里的三层：一场（`wsq先手 vs mzy` … `wsq 2:0 mzy`）、一轮（`round1`）、BP（`先手1ban 阿兰`）。

**加强：不存字段，算出来的** —— 第 n 轮时某一方前面输过就带这个状态。是/否，没有层数。

> ❓ 赢了之后这个状态会消失吗？还是只要输过就一直带着到这场结束？

### cs2

```
cs2_matches  id, date, p1, p2, 备注
cs2_games    对局id, 第几局, 模式, p1得分, p2得分
```

**记真实得分**，不猜规则。模式是打字输入（用过的进候选）。输赢由得分算出来 ——
所以不管你们是打一局定胜负、算总分、还是别的，都不需要我懂。

> ❓ 一场里面有几局？
> ❓ 「solo 三项」记成**一局**（填总分），还是记成**三局**？

## 三、统计表（就这些，其余全删）

全部按你原话里的那几项：

| # | 表 | 行 | 列 |
|---|---|---|---|
| 1 | 角色 BP | 角色 | 出现次数、被 ban 率、被 pick 率、上场率、胜率 |
| 2 | 角色 × BP 位置 | 角色 + 第几个 ban / 第几个 pick | 次数、占比 |
| 3 | 角色 vs 角色 | 角色 A + 角色 B | 轮数、胜率 |
| 4 | 玩家 × 角色 | 玩家 + 角色 | 被 ban 率、被 pick 率、上场率、胜率 |
| 5 | 玩家 vs 玩家 | 玩家 A + 玩家 B | bo3 场次、胜、负、平 |
| 6 | 玩家×角色 vs 玩家×角色 | 玩家 A+角色 A+玩家 B+角色 B | 轮数、胜率 |
| 7 | 胜利条件 | 胜利条件 | 出现次数 |

第 1 表的几个率，分母我打算这么取（这几个数差别很大）：

- 出现次数 / 出现率 —— 分母是**全部对局**
- 被 ban 率、被 pick 率 —— 分母是**这个角色出现的对局**
- 上场率 —— 分母是**这个角色被 pick 的对局**
- 胜率 —— 分母是**这个角色实际上场的轮数**

> ❓ 分母这么取对不对？你只说"上场率"、"胜率"我也拿不准你想除什么。

cs2 的表：

| # | 表 | 行 | 列 |
|---|---|---|---|
| 1 | 各模式 | 模式 | 局数、胜、负、胜率 |
| 2 | 玩家 × 模式 | 玩家 + 模式 | 局数、胜率、场均得分 |
| 3 | 玩家 vs 玩家 | 玩家 A + 玩家 B | 局数、胜、负 |

> ❓ 还想要什么？不想要的直接划掉。

## 四、用词

只用你 log 和你原话里出现过的词：

> 出现 · BP · ban · pick · 1ban · 1pick · 上场 · 轮 · round · 先攻 · 先手 · 后手 ·
> 胜率 · 上场率 · bp率 · bo3 · 战胜 · 积分胜

不用我造的词（漏斗、槽位、顺位、对位、净胜、翻盘率、加强层数、系列赛、被选后上场率）。

表名和列名做成**界面上可改、存数据库**的，改词不用找我。

> ❓ 上面那些表名和列名，你希望具体叫什么？直接用你的话写在这一节，我照抄。

## 五、界面

朴素后台：白底、细边框、普通表格和表单。不搞深色卡片、不搞彩色标签。
页面做到能用的程度，你看完再提。

## 六、代码注释

只解释代码。**不写任何关于游戏规则的注释** —— 规则是你的，不是我的。

## 七、要删掉的东西

推倒重来，旧代码整份不要：`src/games/`、`game-loader.ts` 那套自动发现、
`round-board.ts` 那套复用、15 张统计表、加强层数、cs2 的 BO3 模型。

**保留**：`docs/raw-logs.md`（三份 log 原文 + 人工解析，是新版本的验收数据）。
